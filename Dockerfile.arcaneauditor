FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

COPY ArcaneAuditorCLI /usr/local/bin/ArcaneAuditorCLI

RUN chmod +x /usr/local/bin/ArcaneAuditorCLI

RUN ArcaneAuditorCLI --version

CMD ["/bin/bash"]
